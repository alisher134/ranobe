import { Button } from '@heroui/react';
import { MessagesSquareIcon } from 'lucide-react';

export function ForumDropdown() {
  return (
    <Button variant="ghost">
      <MessagesSquareIcon /> Форум
    </Button>
  );
}
